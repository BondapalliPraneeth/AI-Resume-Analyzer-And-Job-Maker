{
  "name": "resumeai",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix frontend run dev -- --host 0.0.0.0 --port 8080",
    "build": "npm --prefix frontend run build",
    "postinstall": "cd frontend && npm install"
  }
}
